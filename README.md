# T-Garden-SRGs

This repository contains the SRGs calculated for the T-Garden network repository. The files have been compressed using zstd compression, to uncompress, use:

```Bash
zstd -d folder_name.tar.zst
tar -xvf folder_name.tar
```

The T-Garden team

